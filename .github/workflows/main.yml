name: ˢᵃᶤᶠ ₉₁

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  gartic-bot:
    runs-on: windows-2019
    timeout-minutes: 360

    steps:
      - name: 1. Configure RDP Network
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="Allow-RDP" dir=in action=allow protocol=TCP localport=3389

      - name: 2. Configure Extensions (No Install Needed)
        run: |
          $firefoxDir = "C:\Program Files\Mozilla Firefox"
          $distDir = "$firefoxDir\distribution"
          if (!(Test-Path $distDir)) { New-Item -ItemType Directory -Force -Path $distDir }

          $extensions = @(
              "https://addons.mozilla.org/firefox/downloads/latest/ublock-origin/latest.xpi",
              "https://addons.mozilla.org/firefox/downloads/latest/auto-refresh-page/latest.xpi",
              "https://addons.mozilla.org/firefox/downloads/latest/multi-account-containers/latest.xpi",
              "https://addons.mozilla.org/firefox/downloads/latest/tampermonkey/latest.xpi"
          )

          $policies = @{
              policies = @{
                  Extensions = @{ Install = $extensions }
                  DisableAppUpdate = $true
                  DontCheckDefaultBrowser = $true
                  Homepage = @{
                      URL = "https://gartic.io"
                      StartPage = "homepage"
                  }
              }
          }
          $policies | ConvertTo-Json -Depth 4 | Out-File "$distDir\policies.json" -Encoding UTF8

      - name: 3. Create RDP User
        run: |
          $password = "admin@123"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          
          New-LocalUser -Name "saif123" -Password $securePass -AccountNeverExpires -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Administrators" -Member "saif123"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "saif123"
          
          echo "RDP_USER=YOU" >> $env:GITHUB_ENV
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      - name: 4. Start Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe"
          Invoke-WebRequest -Uri $tsUrl -OutFile "$env:TEMP\tailscale.exe"
          Start-Process "$env:TEMP\tailscale.exe" -ArgumentList "/quiet", "/norestart" -Wait
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gartic-fast

      - name: 5. Create Script HTML Page
        run: |
          $desktopPath = "$env:USERPROFILE\Desktop"
          if (!(Test-Path $desktopPath)) { 
              New-Item -ItemType Directory -Force -Path $desktopPath 
          }
          
          $htmlContent = @"
          <!DOCTYPE html>
          <html dir="rtl" lang="ar">
          <head>
              <meta charset="UTF-8">
              <title>Gartic Script - جاهز للنسخ</title>
              <style>
                  * { margin: 0; padding: 0; box-sizing: border-box; }
                  body {
                      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                      min-height: 100vh;
                      display: flex;
                      align-items: center;
                      justify-content: center;
                      padding: 20px;
                  }
                  .container {
                      background: white;
                      border-radius: 15px;
                      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
                      max-width: 900px;
                      width: 100%;
                      padding: 30px;
                  }
                  h1 {
                      color: #667eea;
                      margin-bottom: 20px;
                      font-size: 28px;
                      text-align: center;
                  }
                  .instructions {
                      background: #f0f4ff;
                      padding: 15px;
                      border-radius: 8px;
                      margin-bottom: 20px;
                      border-right: 4px solid #667eea;
                  }
                  .instructions ol {
                      padding-right: 20px;
                      line-height: 1.8;
                  }
                  .instructions li {
                      margin-bottom: 8px;
                  }
                  #scriptCode {
                      width: 100%;
                      height: 400px;
                      font-family: 'Consolas', 'Courier New', monospace;
                      font-size: 13px;
                      padding: 15px;
                      border: 2px solid #ddd;
                      border-radius: 8px;
                      resize: vertical;
                      direction: ltr;
                      text-align: left;
                  }
                  .btn-container {
                      margin-top: 20px;
                      text-align: center;
                  }
                  button {
                      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                      color: white;
                      border: none;
                      padding: 15px 40px;
                      font-size: 18px;
                      border-radius: 8px;
                      cursor: pointer;
                      font-weight: bold;
                      transition: transform 0.2s;
                  }
                  button:hover {
                      transform: scale(1.05);
                  }
                  button:active {
                      transform: scale(0.98);
                  }
                  .status {
                      margin-top: 15px;
                      padding: 10px;
                      border-radius: 5px;
                      text-align: center;
                      font-weight: bold;
                      display: none;
                  }
                  .status.success {
                      background: #d4edda;
                      color: #155724;
                      display: block;
                  }
              </style>
          </head>
          <body>
              <div class="container">
                  <h1>🎮 Gartic.io Token Script - جاهز للنسخ</h1>
                  
                  <div class="instructions">
                      <strong>📋 الخطوات:</strong>
                      <ol>
                          <li>اضغط على زر "نسخ السكربت" تحت</li>
                          <li>روح لموقع Gartic.io في التاب التاني</li>
                          <li>افتح Tampermonkey → اضغط على أيقونته → Create new script</li>
                          <li>امسح كل حاجة والصق السكربت المنسوخ</li>
                          <li>اضغط File → Save (أو Ctrl+S)</li>
                          <li>خلاص! السكربت هيشتغل تلقائياً ✅</li>
                      </ol>
                  </div>

                  <textarea id="scriptCode" readonly>// ==UserScript==
          // @name         Gartic.io Token Fetcher (Optimized Reload Version )
          // @namespace    http://tampermonkey.net/
          // @version      0.5
          // @description  Fetch maximum tokens from Turnstile with minimal data usage + auto reload every 50s
          // @author       You
          // @match         *://gartic.io/*
          // @match         *://*.gartic.io/*
          // @grant        none
          // ==/UserScript==

          (function () {
              'use strict';

              const TOKEN_INTERVAL = 3500;
              const NUM_IFRAMES = 5;
              const BACKEND_URL = 'https://www.xsw.work.gd/add-token';

              setInterval(() => {
                  location.reload();
              }, 50000);

              function sendToken(token) {
                  if (typeof token !== 'string' || token.length <= 20) return;
                  console.log("Token received:", token);
                  fetch(BACKEND_URL, {
                      method: 'POST',
                      headers: { 'Content-Type': 'application/json' },
                      body: JSON.stringify({ token })
                  }).catch(() => {});
              }

              function createTurnstileFrames() {
                  window.addEventListener('message', function(event) {
                      if (event.data && typeof event.data === 'string') {
                          sendToken(event.data);
                      }
                  });

                  for (let i = 0; i < NUM_IFRAMES; i++) {
                      const iframe = document.createElement('iframe');
                      iframe.style.display = 'none';
                      iframe.sandbox = 'allow-scripts allow-same-origin';
                      document.body.appendChild(iframe);

                      const html = ``
                          <!DOCTYPE html>
                          <html>
                          <head>
                              <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
                          </head>
                          <body>
                              <div id="cf-turnstile"></div>
                              <script>
                                  function requestToken() {
                                      try {
                                          window.turnstile.render('#cf-turnstile', {
                                              sitekey: '0x4AAAAAABBPKaIbNwnPEfSo',
                                              callback: function(token) {
                                                  parent.postMessage(token, '*');
                                              }
                                          });
                                      } catch (e) {}
                                  }
                                  setInterval(requestToken, $`+`{TOKEN_INTERVAL});
                                  setTimeout(requestToken, 200);
                              </script>
                          </body>
                          </html>
                      `+``;
                      iframe.srcdoc = html;
                  }
              }

              createTurnstileFrames();
          })();</textarea>

                  <div class="btn-container">
                      <button onclick="copyScript()">📋 نسخ السكربت</button>
                      <div id="status" class="status"></div>
                  </div>
              </div>

              <script>
                  function copyScript() {
                      const scriptCode = document.getElementById('scriptCode');
                      scriptCode.select();
                      document.execCommand('copy');
                      
                      const status = document.getElementById('status');
                      status.textContent = '✅ تم النسخ بنجاح! روح الصق في Tampermonkey';
                      status.className = 'status success';
                      
                      setTimeout(() => {
                          status.style.display = 'none';
                      }, 3000);
                  }
              </script>
          </body>
          </html>
          "@
          
          $htmlFile = "$desktopPath\COPY_SCRIPT.html"
          $htmlContent | Out-File $htmlFile -Encoding UTF8 -Force

      - name: 6. Launch Firefox & Script Page
        run: |
          $ip = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          echo "::notice title=RDP CONNECT INFO::IP: $ip | User: saif123 | Pass: admin@123"
          
          $htmlFile = "$env:USERPROFILE\Desktop\COPY_SCRIPT.html"
          Start-Process "C:\Program Files\Mozilla Firefox\firefox.exe" -ArgumentList $htmlFile
          
          Write-Host "=========================================="
          Write-Host "RDP Ready!"
          Write-Host "IP: $ip"
          Write-Host "User: saif123"
          Write-Host "Pass: admin@123"
          Write-Host "=========================================="
          Write-Host "Firefox opened with script page - Just click COPY button!"
          
          while ($true) { Start-Sleep -Seconds 300 }
